table
  tr
    th 区域编码
    th 区域名称
    th 状态
    th 城市
    th colspan='3'

  - @districts.each do |district|
    tr
      td = district.district_code
      td = district.district_name
      td = district.status_text
      td = district.city.try(:city_name)
      td = link_to '显示', district
      td = link_to '编辑', edit_district_path(district)
      td = link_to '删除', district, :confirm => '确定要删除吗?', :method => :delete
br

= link_to '新增区域', new_district_path
