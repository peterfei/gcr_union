h1 Listing base_rate_codes

table
  tr
    th Rate code
    th Rate code name
    th Base hour
    th Base km
    th
    th
    th

  - @base_rate_codes.each do |base_rate_code|
    tr
      td = base_rate_code.rate_code
      td = base_rate_code.rate_code_name
      td = base_rate_code.base_hour
      td = base_rate_code.base_km
      td = link_to 'Show', base_rate_code
      td = link_to 'Edit', edit_base_rate_code_path(base_rate_code)
      td = link_to 'Destroy', base_rate_code, :confirm => 'Are you sure?', :method => :delete

br

= link_to 'New Base rate code', new_base_rate_code_path
